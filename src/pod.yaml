status:
  availableReplicas: 2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: py-serv
  template:
    spec:
      containers:
        - name: py-serv
          image: ghcr.io/metalbear-co/mirrord-pytest:latest
